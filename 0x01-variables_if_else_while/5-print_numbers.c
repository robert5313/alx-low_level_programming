#include <stdio.h>

/**
 * main - Entry point
 * author: Robert
 * Return: base 0 (pass)
 */
int main(void)
{
	int big;

	for (big = 0; big < 10; big++)
	{
		printf("%d", big);
	}
	printf("\n");
	return (0);
}
